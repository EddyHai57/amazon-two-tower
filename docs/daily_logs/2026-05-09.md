# 2026-05-09 日志

## 环境与连接

- 已租用 Vast.ai RTX 3090 服务器，并通过 VSCode Remote SSH 连接。
- PyTorch/CUDA/GPU 验证成功：
  - torch 2.11.0+cu126
  - CUDA 可用：True
  - CUDA 12.6
  - GPU NVIDIA GeForce RTX 3090

## 项目进展

- GitHub 仓库 `/workspace/tianchi-two-tower` 已成功克隆。
- 已创建 `/workspace/AGENTS.md`，用于记录 Codex 工作规则。
- 已创建 `/workspace/amazon-two-tower`，作为 `/workspace/tianchi-two-tower` 的同级项目。
- 已创建初始提交：
  - `82d2fac amazon-two-tower: initial skeleton and inspection script`

## 数据检查状态

- 初次运行数据集检查失败，原因是 HuggingFace datasets 不再支持数据集脚本：

```text
Dataset scripts are no longer supported, but found Amazon-Reviews-2023.py
```

- 该问题不是 Amazon 数据本身不可用，也不是服务器问题；当前判断是主环境缺少合适的 datasets 版本，并且新版 datasets 对 HuggingFace dataset script 支持存在兼容性问题。
- 已在 `/workspace/amazon-two-tower` 内创建项目独立虚拟环境 `.venv`。
- 激活 `.venv` 后安装兼容依赖：
  - `datasets==2.17.0`
  - `huggingface_hub==0.36.2`
  - `pyyaml`
  - `pandas`
  - `pyarrow`
- 依赖版本验证成功：
  - python 3.12.13
  - datasets 2.17.0
  - huggingface_hub 0.36.2
  - pandas 3.0.2
  - pyarrow 24.0.0
- 使用 `.venv` 重新运行 inspection 脚本：

```bash
python scripts/inspect_amazon_dataset.py --config configs/amazon_all_beauty_phase1.yaml
```

- inspection 脚本已成功完成，并生成：
  - `outputs/inspection_all_beauty.md`

## 数据检查结果

- loading strategy used：`full_load`
- review columns：

```json
["rating", "title", "text", "images", "asin", "parent_asin", "user_id", "timestamp", "helpful_vote", "verified_purchase"]
```

- meta columns：

```json
["main_category", "title", "average_rating", "rating_number", "features", "description", "price", "images", "videos", "store", "categories", "details", "parent_asin", "bought_together", "subtitle", "author"]
```

- review row count：701528
- meta row count：112590
- unique user_id count：631986
- unique parent_asin count：112565

## 当前结论

- Amazon All_Beauty 数据已经成功下载和加载。
- review 数据可用。
- meta 数据可用。
- `user_id` 字段存在，可作为用户原始 ID。
- `parent_asin` 字段存在，可作为召回 item 粒度。
- `rating` 字段存在，可用于构造隐式反馈。
- `timestamp` 字段存在，可用于时间切分。
- 没有创建 train/valid/test 文件。
- 没有生成 user/item id mapping。
- 没有实现 ItemCF 或 two-tower。
- 没有生成模型 checkpoint 或 text embeddings。

## Git 状态处理

- 发现 `.venv/` 出现在 `git status` 未追踪文件中。
- 已将 `.venv/` 加入 `.gitignore`，避免提交虚拟环境。
- 当前 `git status` 中有：
  - 修改：`.gitignore`
  - 未追踪：`docs/LOGGING_GUIDE.md`
  - 未追踪：`outputs/inspection_all_beauty.md`
- `outputs/inspection_all_beauty.md` 是运行产物，暂时不要提交，除非 Eddy 明确要求。
- `docs/LOGGING_GUIDE.md` 和 `.gitignore` 可以作为文档或配置变更提交。
