# 2026-06-03 Daily Log

## 任务目标

完成 LogQ / Uber BatchQ 调查收口，并将学习文档改成更适合离线阅读的中文解释版。

## 服务器最终状态

Vast.ai 队列 `transformer_sampling_uber_alpha010_full` 已按预注册 gate 自动停止：

```text
tmux: gone (expected after gate stop)
GPU: idle
repo: clean, HEAD=33053a9
status: stopped_after_alpha010_seed2024_full_audit
```

未启动：

```text
4ch
Faiss
canonical replacement
seed2025
new alpha sweep
```

## Uber BatchQ alpha=0.10 验收结果

### Gate0: alpha=0 sanity

```text
alpha=0.00 full test R@50 = 0.103116
canonical full test R@50  = 0.103168
absolute delta            = 0.000052 < 0.001
```

结论：LogQ / BatchQ 代码分支在 `alpha=0` 时能复现基础 InfoNCE 口径，baseline 锚定干净。

### Gate1: alpha=0.10 seed42

```text
full test R@50 = 0.112047
overall delta point estimate = +0.008879
overall CI95 = [0.008311, 0.009461]
long-tail delta point estimate = +0.002055
long-tail CI95 = [0.001171, 0.002964]
Top50 >100 share = 24.80%
Gini = 0.6697
```

结论：seed42 看起来是健康改进，总 Recall 和长尾均提升。

### Gate2: alpha=0.10 seed2024

```text
full test R@50 = 0.114982
overall delta point estimate = +0.011278
overall CI95 = [0.010895, 0.011660]
1-5 bucket delta = -0.004252
6-20 bucket delta = -0.002779
long-tail delta point estimate = -0.003202
long-tail CI95 = [-0.003751, -0.002670]
Top50 >100 share = 26.92%
Gini = 0.6919
```

失败约束：

```text
bucket_1-5
bucket_6-20
```

结论：seed2024 总 Recall 继续提升，但低热度目标桶显著回退。长尾 CI95 整体为负，说明退化不是随机噪声。

## 收口决策

最终不采用 LogQ / Uber BatchQ：

```text
Accepted canonical:
  Time-aware Transformer Two-Tower, base InfoNCE
  + 4ch valid-selected Weighted RRF
  + Faiss as ANN acceleration only

Rejected / diagnostic:
  old empirical LogQ alpha=1.0
  old empirical LogQ alpha=0.25
  Uber BatchQ alpha=0.25
  Uber BatchQ alpha=0.10
  Refined LogQ / MNS current smoke settings
```

原因：

- 总 Recall 提升真实存在。
- 强 correction 会造成热门曝光集中和 coverage 下降。
- 温和 `alpha=0.10` 的“长尾健康”不跨 seed 稳定。
- 继续调 alpha 容易变成事后搜索，不符合当前收口目标。

## 文档更新

更新文件：

```text
docs/learning_guide.md
docs/reports/transformer_logq_investigation.md
docs/project_summary.md
docs/decision_log.md
docs/issue_log.md
docs/daily_logs/2026-06-03.md
```

学习指南重点改动：

- 新增“终极模型是什么 / LogQ 用没用 / Faiss 是不是模型一部分”。
- 新增术语速查，解释 OOV、cold item、seen mask、InfoNCE、LogQ、RRF、Faiss、ANN、Gini、entropy、bootstrap CI 等。
- 将 Uber BatchQ `alpha=0.10` 从 pending 改为 rejected by seed2024 long-tail gate。
- 明确最终主线不含 LogQ。

## 面试口径

推荐说法：

```text
我最终没有采用 LogQ。它确实能提高总 Recall，但强修正会把收益转移到热门商品。
温和的 Uber BatchQ alpha=0.10 在 seed42 上总 Recall 和长尾都提升，
但 multi-seed 验证时 seed2024 暴露了显著长尾退化。
所以我按预注册 gate 拒绝它，保持基础 InfoNCE Transformer + 4ch RRF 为最终主线。
这个负结果说明我不会只追单一 Recall，而会同时看长尾、coverage、多 seed 和 bootstrap CI。
```
